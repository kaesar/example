name: CI with Node.js

on:
  pull_request:
    branches: [ master ]

jobs:
  ci_to_master:
    runs_on: debian-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v2
        with:
          node-version: 12
      - run: node app.js
